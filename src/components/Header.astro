---
const navLinks = [
  { href: '/features', label: 'Features' },
  { href: '/how-it-works', label: 'How It Works' },
  { href: '/about', label: 'About' },
];
---

<header class="fixed top-0 left-0 right-0 z-50 bg-cream/90 backdrop-blur-md border-b border-rose/30">
  <nav class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <!-- Logo -->
      <a href="/" class="flex items-center gap-2 text-walnut hover:text-walnut">
        <span class="text-xl font-bold font-serif">Annie's Journal</span>
      </a>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center gap-8">
        {navLinks.map(link => (
          <a
            href={link.href}
            class="text-walnut-muted hover:text-sage font-medium transition-colors"
          >
            {link.label}
          </a>
        ))}
      </div>

      <!-- CTA Button -->
      <div class="flex items-center gap-4">
        <a
          href="https://app.anniesjournal.com"
          class="hidden sm:inline-flex btn-primary text-sm py-2 px-4"
        >
          Open App
        </a>

        <!-- Mobile Menu Button -->
        <button
          type="button"
          class="md:hidden p-2 text-walnut-muted hover:text-sage rounded-lg transition-colors"
          aria-label="Open menu"
          id="mobile-menu-button"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </nav>

  <!-- Mobile Menu -->
  <div id="mobile-menu" class="hidden md:hidden bg-cream border-t border-rose/30">
    <div class="px-4 py-4 space-y-3">
      {navLinks.map(link => (
        <a
          href={link.href}
          class="block text-walnut-muted hover:text-sage font-medium py-2 transition-colors"
        >
          {link.label}
        </a>
      ))}
      <a
        href="https://app.anniesjournal.com"
        class="block w-full text-center btn-primary"
      >
        Open App
      </a>
    </div>
  </div>
</header>

<script>
  const menuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');

  menuButton?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });
</script>
